// null and undefined. null value is a value that is deliberate(something doesnt exist). 
// undefined is a variable that does not have any value

let user = null;
console.log(user);

let age;
console.log(age);
